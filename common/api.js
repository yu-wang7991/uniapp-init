const { http } = uni.$u

// config = { custom: {return:true } }处理没有code的情况

// export const test = (params, config = {}) => http.post('/ebapi/public_api/index', params, config)

// export const test1 = (params, config = {}) => http.get('/ebapi/public_api/index', { params }, config)